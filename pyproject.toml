[project]
name = "ozon-promotion-mvp"
version = "0.1.0"
description = "MVP scaffold for Ozon trend capture to Xiaohongshu publishing"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["app"]

